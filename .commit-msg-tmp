wave 6a foundation: multi-run refactor + 2 renames + drift gate 5-class + V1_4_BRIEF items 2/4/5/6 + 7 CIDOCS-F items

v1.4 dogfood-swarm Wave 6a foundation — the lopsided wave (backend
refactor-heavy + ci-docs mechanical-bulk + bridge/tests light + frontend
mid). 5-agent parallel dispatch closes 12+ load-bearing items + 5 v1.5
candidates documented. All Wave 1-5.5 contracts preserved per advisor's
explicit "preserve EVERY contract" tripwire.

26 files changed; +5300 / -1900 net (includes the 750-LOC drift gate
extension + the 320-LOC prep_release.sh script + the 4-test-class +
4-test-class + 21-test new test files). +71 net tests (2152 → 2223).

## Backend — multi-run refactor + RUNTIME_GPU_OOM Option A wrap

Closes BACKEND-A-003 + A-004 + B-002 + BACKEND-A-001 RUNTIME_GPU_OOM
contract drift, all 4 coupled per advisor lock 2026-05-25.

- Extracted shared `_build_sft_config()` helper in trainer.py (~120 LOC)
  + `_apply_train_on_responses_only()` helper (~60 LOC) — single source
  of truth for SFTConfig assembly with v1.3 BACKEND-5/7 paged-optim +
  Ada bf16/fp16 autodetection + train_on_responses_only Unsloth masking.
- multi_run.py `_execute_run` now calls both helpers; pre-fix it built
  SFTConfig inline, silently skipping the v1.3 quality contracts.
- BACKEND-B-002 gate: `if self.config.save_every_run and not run_failed:`
  on the multi_run save+register branch — failed runs no longer poison
  resume state.
- RUNTIME_GPU_OOM Option A wrap: trainer.py oom_recovery=False branch
  raises `GPUMemoryError(code='RUNTIME_GPU_OOM')` (collapses the
  Wave 2 split-state forward — user-facing docs become accurate WITHOUT
  doc changes). multi_run.py multi-run symmetric writes
  `RUNTIME_GPU_OOM:` prefix on failure_reason.
- 16 new tests across test_trainer.py (10) + test_multi_run.py (6);
  1 existing test updated (test_oom_recovery_false_reraises_immediately
  now expects GPUMemoryError).
- All Wave 1-5.5 contracts preserved (auth middleware regression set,
  OOM auto-recovery loop, RUNTIME_OOM_RECOVERY_EXHAUSTED + RUNTIME_OOM_
  ADJACENT codes, atomic checkpoints, SLAO merge ordering, F-002 resume,
  TrainingCallback contract, MultiRunConfig dataclass fields).

## Bridge — BRIDGE-A-002 follow-up (5 Wave 6b kwargs on Trainer.__init__)

Closes BRIDGE-A-002 follow-up per WAVE_6A_TODO.md.

- Added `use_dora` / `packing` / `init_lora_weights` / `lora_preset` /
  `optim` kwargs to Trainer.__init__ with `None` defaults + settings
  fallback. Backward-compat: callers not passing these kwargs see
  byte-identical pre-fix behavior.
- Added the same 5 fields to MultiRunConfig dataclass + threaded
  through MultiRunTrainer.__init__ → inner Trainer constructor.
- Composed cleanly with BACKEND's _build_sft_config extension: the
  helper gains an optional `optim` parameter that consumes self.optim
  or settings.training.optim.
- 15 new tests across test_trainer.py + test_multi_run.py for the
  constructor + settings-fallback + introspection + end-to-end CLI →
  constructor wire-through.

## Tests — TESTS-A-007 next-batch serial markers

Closes the TESTS-A-007 / TESTS-B-016 follow-up per WAVE_6A_TODO.md.

- Added `@pytest.mark.serial` to 6 test classes in test_logging_config.py
  (4 named + 2 grep-doctrine siblings). TestStructlogAvailable EXEMPT
  (pure-read; no process-global mutation).
- `pytest-xdist` parallel-flip-on NOT activated this wave (v1.5 candidate
  per advisor lock — markers ship in v1.4; flip-on bedded in v1.5+).

## CI/Docs — drift gate 5-class extension + V1_4_BRIEF items 2/4/5/6 + 7 CIDOCS-F items

Closes V1_4_BRIEF items 2/4/5/6 + CIDOCS-F-006/007/010/022/023/024/025.

- Drift gate (`scripts/check_doc_drift.py`) gained 5 new scanner classes
  (Classes 1-5 per Wave 5 Decision 3 ALL_5_CLASSES lock): argparse
  defaults / env-var names in llms.txt / env-var defaults / error-codes
  fix references / CI workflow severity claims. ~750 LOC + 21 unit tests
  in tests/test_check_doc_drift.py.
- COORDINATOR VERIFY-PAUSE result: Drift check passed: 0 items. Extension
  caught 2 REAL drifts in-wave (BACKPROPAGATE_DATA__PACKING + --max-seq-
  length) and fixed them (env-vars.md + error-codes.md). 1 legitimate
  allow-list entry added (--host argparse fallthrough).
- verify-smoke gate flipped from soft (continue-on-error: true) to strict
  with jq-based parse (V1_4_BRIEF item 2 / CIDOCS-F-006).
- Bandit gate doc-lie fixed in copy (LOW/LOW matches `-l -i` flags); the
  flag-bump to MEDIUM/MEDIUM deferred to v1.5 with rationale (V1_4_BRIEF
  item 4).
- New scripts/prep_release.sh (~320 LOC) — 8-stage pre-release coordinator
  combining CITATION.cff bump + translation invocation + PKG-INFO smoke +
  drift gate + verify.sh + twine check + reference list validation.
- New scripts/preflight.sh (~228 LOC) — read-only local CI mirror with
  --quick fast-loop flag.
- Pre-commit hook for check_doc_drift.py on pre-push stage.
- PyPI PKG-INFO smoke step added to build job (CIDOCS-F-007).
- uv sync --frozen pilot on Linux 3.11 (CIDOCS-F-023); strict + matrix
  migration deferred to v1.5.
- mutmut workflow documented with manual baseline-bootstrap trigger flow
  (CIDOCS-F-022).

## Frontend — ui_security Gradio→UI + TRAINING_PRESETS → MULTI_RUN_PRESETS

Closes V1_4_BRIEF item 7 (partial) + Wave 5 Decision 3.

- 4 ui_security helpers renamed: safe_gradio_handler → safe_ui_handler,
  raise_gradio_{error,warning,info} → raise_ui_{error,warning,info}.
- RequestContext.from_gradio_request → from_request with classmethod
  deprecation shim.
- Module-level `__getattr__` (PEP 562) in ui_security.py emits
  DeprecationWarning on legacy access with v1.4 → v1.5 → v1.6 cycle text.
- TRAINING_PRESETS → MULTI_RUN_PRESETS rename (config.py); legacy name
  resolves via module-level `__getattr__` + DeprecationWarning mentioning
  LORA_PRESETS for disambiguation.
- 3-version deprecation cycle (v1.4 DeprecationWarning → v1.5
  UserWarning → v1.6 removal) per advisor lock Q4.
- migrations.md handbook page updated with v1.3 → v1.4 section covering
  both renames + the cycle table.
- 18 new tests across tests/test_ui_security_legacy_aliases.py (10) +
  tests/test_config_legacy_aliases.py (8).
- PATH D escalation: `_GradioShim` deletion DEFERRED — the class is
  load-bearing today (module body has ~20 `gr.Request` type hints +
  10 `gr.Error()` raises). Deletion cascades into broader refactor;
  flagged in WAVE_6A_TODO.md as Wave 6b / v1.5 candidate.

## Coordinator post-amend fixes (build verification)

5 test-assertion-text mismatches from refactor caught by pytest:

- test_multi_run.py TestMultiRunTrainerDocstringMentionsRuntimeGpuOom:
  removed orphan `assert callable(multi_run._build_abort_callback)` line
  (copy-paste artifact from BACKEND agent test).
- test_multi_run.py test_wave6b_kwargs_forwarded_to_inner_trainer: patch
  path corrected from `backpropagate.multi_run.Trainer.__init__` to
  `backpropagate.trainer.Trainer.__init__` (Trainer is lazy-imported
  inside run(), not a module attribute of multi_run).
- test_wave6b_flags.py test_replay_multi_run_override_applies_to_mr_
  trainer_kwargs: assertion updated to read `init_kwargs["config"]
  .use_dora` (post-BRIDGE-A-002 the Wave 6b kwargs live on MultiRunConfig,
  not direct MultiRunTrainer kwargs).
- test_windows_compat.py test_training_uses_zero_workers_on_windows:
  literal-match updated to canonical dict-key form `"dataloader_num_
  workers": 0 if os.name == "nt" else 4` (was `dataloader_num_workers=0`
  — kwarg-style; BACKEND refactor moved it into _build_sft_config dict).
- ruff --fix on import ordering in new test files (6 errors auto-fixed).

## Build verification

- ruff check . → clean
- python -m mypy backpropagate/ → 0 errors (48 source files)
- pytest tests/ → 2223 passed, 8 skipped, 0 failed (+71 from Wave 6a.0)
- python scripts/check_doc_drift.py → 0 items (5-class scanner)

26 files changed. Net +5300 / -1900 LOC.

## Wave 6b carry-forward (next wave dispatches on this merge)

Per WAVE_6A_TODO.md remaining items:
- mode="full" for ≤3B models (V1_4_BRIEF item 8, ~500-800 LOC; Decision 1 B locked)
- ollama register triad (`backprop ollama register|list|rm`; ~310 LOC; Decision 4 locked)
- 6 Tests Wave 5 HIGH coverage gaps (TESTS-F-001..006)
- BRIDGE-F-002 log flags / F-007 --json on validate+replay / F-010..015
- FRONTEND-F-004 push-to-hub UI / F-005 dataset stats grid
- BACKEND-F-002 VRAM pre-flight / F-003 MultiRunTrainer callback parity
  / F-004 CheckpointManager filelock parity
- _GradioShim deletion (Path D escalation from this wave)

Estimated Wave 6b scope: ~1100-1700 LOC, ~25 items (per Wave 5 synthesis
Decision 1 B sizing).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
